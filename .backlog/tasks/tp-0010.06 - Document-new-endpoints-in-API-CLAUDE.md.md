---
id: TP-0010.06
title: Document new endpoints in API CLAUDE.md
status: Done
assignee: []
created_date: '2026-05-23 21:50'
updated_date: '2026-05-23 22:31'
labels: []
dependencies: []
parent_task_id: TP-0010
priority: low
ordinal: 29000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Update src/routes/api/CLAUDE.md to document the new endpoints.

Add to the Authentication section:
- POST /api/authentication/verify-email (Public) — body {token}; returns {ok, userId}
- POST /api/authentication/verify-email/resend (user) — returns {ok}
- POST /api/admin/emails/send (admin) — body {to, templateKey, link?}; returns {ok}

Add a new section for Email Templates (admin):
- GET /api/email-templates → EmailTemplate[]
- PUT /api/email-templates — body EmailTemplate; returns {id}
- GET /api/email-templates/[id] → EmailTemplate
- POST /api/email-templates/[id] — body EmailTemplate; returns 200 empty
- DELETE /api/email-templates/[id] — returns 200 empty

Match the existing table style.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 All five new endpoints are listed in src/routes/api/CLAUDE.md with auth, body, and return shape
- [x] #2 Email Templates section matches the table style of Items/Skills sections
<!-- AC:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Updated src/routes/api/CLAUDE.md. Added /api/authentication/verify-email (Public) and /api/authentication/verify-email/resend (user) to the Authentication table. Added a new 'Email & Admin' section for /api/admin/emails/send (admin) and /api/admin/users/[id]/resend-verification (admin — was missing from the original task scope but added since it ships in this feature). Added a new 'Email Templates' section with short prose about docUrl + hardcoded subjects + [[LINK]] marker, plus the GET/PUT/GET/POST/DELETE table matching the style of Items/Skills.
<!-- SECTION:FINAL_SUMMARY:END -->
