---
id: TP-0012.10
title: >-
  Apply per-character budget bonus and discounts to budget meter in
  character-version-shop
status: Done
assignee: []
created_date: '2026-05-30 15:50'
updated_date: '2026-05-30 19:56'
labels: []
dependencies: []
parent_task_id: TP-0012
priority: high
ordinal: 51000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The remaining / budget meter in character-create-nav.svelte is fed by character-version-shop.svelte and register-step-create-character.svelte. Two corrections needed:

1. Budget: In register-step-create-character.svelte, after loading the event, also fetch the per-character extra budget via GET /api/events/[eventId]/budget/characters/[characterId] and add it to the base event budget before passing it as the budget prop to character-version-shop.svelte.

2. Spent: In character-version-shop.svelte, apply active discounts when computing skillsSpent, itemsSpent, and implantsSpent so that discounted costs are reflected in remaining.

Key files:
- src/lib/codex/components/register-step-create-character.svelte — fetches event budget, needs to add per-character bonus
- src/lib/codex/components/character-version-shop.svelte — computes spent totals (lines 27–45), needs discount-aware cost maps
- src/lib/db/event_budget.repo.ts — getBudgetForCharacter(eventId, characterId) returns the extra budget amount
<!-- SECTION:DESCRIPTION:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Updated budget GET endpoint to allow user role. Updated company discounts GET endpoint to allow user role. In register-step-create-character.svelte, after loading the event, also fetches extra budget from /api/events/[eventId]/budget/characters/[characterId] (when character.id is known) and adds it to the base event budget. In character-version-shop.svelte, added a reactive  that fetches company discounts whenever version.company changes, then applied those discounts (flat reduction) to skillsSpent, itemsSpent, and implantsSpent calculations.
<!-- SECTION:FINAL_SUMMARY:END -->
