---
id: TP-0016.05
title: Point register-step-confirm.svelte at the new form-submit endpoint
status: Done
assignee: []
created_date: '2026-05-31 19:28'
updated_date: '2026-05-31 19:33'
labels:
  - frontend
  - codex
dependencies:
  - TP-0016.04
parent_task_id: TP-0016
priority: medium
ordinal: 68000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Update the registration wizard's confirm step to call the new auth-gated endpoint instead of the old public scrape-based one. The path uses `selectedEventId` (no longer `selectedFormId` in the path); body and error handling are unchanged.

Files:
- `site/src/lib/codex/components/register-step-confirm.svelte` (lines 47–62)
  - Change `fetch(\`/api/forms/\${formId}/submit\`, ...)` to `fetch(\`/api/my/events/\${REGISTER_MANAGER.selectedEventId}/form-submit\`, ...)`
  - Keep the existing `if (formId)` guard so events without a linked form skip the call entirely
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Confirming registration for a form-linked event posts to /api/my/events/{eventId}/form-submit
- [x] #2 Confirming for an event without a linked form skips the submit call and proceeds straight to the participant PUT
- [x] #3 Submission errors render the existing fallback link to the form's responderUri
<!-- AC:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Updated register-step-confirm.svelte to POST to /api/my/events/${selectedEventId}/form-submit instead of the old /api/forms/${formId}/submit. The if (formId) guard, body, and fallback error handling are unchanged.
<!-- SECTION:FINAL_SUMMARY:END -->
