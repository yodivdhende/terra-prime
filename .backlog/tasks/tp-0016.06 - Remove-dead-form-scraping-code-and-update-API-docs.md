---
id: TP-0016.06
title: Remove dead form-scraping code and update API docs
status: Done
assignee: []
created_date: '2026-05-31 19:28'
updated_date: '2026-05-31 19:35'
labels:
  - cleanup
  - backend
dependencies:
  - TP-0016.05
parent_task_id: TP-0016
priority: low
ordinal: 69000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Now that submission goes through the Sheets-backed endpoint, remove the unused scrape path:

- Delete `site/src/routes/api/forms/[formId]/submit/+server.ts`
- From `site/src/lib/services/google-form-service.ts` remove `submitForm`, `parseEntryIds`, `buildEntryIdMap`, and their console logging. Keep `getForm` — still used by `/api/forms/[formId]/+server.ts` for the admin/registration UI to fetch form structure.
- Trim the Forms table in `site/src/routes/api/CLAUDE.md` to remove the deleted endpoint and reflect the new auth-gated submit under the "My" section.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Old `/api/forms/[formId]/submit` route file no longer exists
- [x] #2 `google-form-service.ts` only exposes `getForm` plus its types
- [x] #3 `site/src/routes/api/CLAUDE.md` no longer documents POST /api/forms/[formId]/submit and lists POST /api/my/events/[eventId]/form-submit instead
<!-- AC:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Deleted src/routes/api/forms/[formId]/submit. Trimmed submitForm, parseEntryIds, buildEntryIdMap from google-form-service.ts (only getForm + types remain). Updated routes/api/CLAUDE.md: Forms table now lists only GET; added POST /api/my/events/[eventId]/form-submit under My; mentioned sheetId on the Events section. Also updated form-window.svelte (standalone playtest view) to open responderUri in a new tab instead of calling the gone scrape endpoint.
<!-- SECTION:FINAL_SUMMARY:END -->
