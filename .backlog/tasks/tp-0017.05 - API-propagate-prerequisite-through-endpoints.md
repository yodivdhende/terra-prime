---
id: TP-0017.05
title: API — propagate prerequisite through endpoints
status: Done
assignee: []
created_date: '2026-06-02'
updated_date: '2026-06-02'
labels:
  - backend
dependencies:
  - TP-0017.02
parent_task_id: TP-0017
priority: medium
ordinal: 75000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Ensure the API routes pass `prerequisite` through to the repo on both create and update, and return it on GET.

Files:
- `site/src/routes/api/implants/+server.ts` (PUT — create)
  - `prerequisite` is included in the request body; `isImplants()` already accepts it after the type update, so no extra validation needed — just confirm `implantRepo.save()` receives it

- `site/src/routes/api/implants/[id]/+server.ts` (POST — update, GET — read)
  - Same: body includes `prerequisite`; repo save and GET response include the field automatically after repo changes

No logic changes required in the route handlers themselves if the `Implant` type and repo are updated correctly — this task is a verification/smoke-test task to confirm end-to-end correctness.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 `GET /api/implants` returns `prerequisite` (null or number) for every implant
- [ ] #2 `POST /api/implants/:id` with a `prerequisite` value saves it to the DB
- [ ] #3 `PUT /api/implants` (create) with a `prerequisite` value saves it to the DB
<!-- AC:END -->
