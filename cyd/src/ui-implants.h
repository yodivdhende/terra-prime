#ifndef UI_IMPLANTS
#define UI_IMPLANTS

/**
 * Give the generated Implants screen its contents. Call once, after `ui_init()`: the screen
 * refills itself from `api/my/implants` every time it is shown.
 */
void uiImplantsInit();

#endif
